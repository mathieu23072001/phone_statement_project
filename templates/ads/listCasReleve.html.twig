{% extends "baseSupAd.html.twig" %}

{% block stylesheets %} {{  parent() }} {% endblock %}

{% block title %} {{  parent() }} {% endblock %}

{% block javascript %} {{  parent() }} {% endblock %}


{% block body %}


 {% for membre in app.user.membres %}

<div class="row bg-title">
 
                <div class="col-lg-3 col-md-4 col-sm-4 col-xs-12">
                    <h4 class="page-title">Centre : {{ membre.cds.nom}}</h4>
                </div>
                <div class="col-lg-9 col-sm-8 col-md-4 col-xs-12"> 
                    <o class="breadcrumb">
                        <li>Agent de sante </li>
                        <li class="active"> Liste cas contact releve</li>
                    </o>
                </div>
                
                <!-- /.col-lg-12 -->
            </div>
  

               



               <div class="row">
                    <div class="col-sm-12">
                        <div class="white-box">
                            <h3 class="box-title m-b-0">Cas contact</h3>
                            <p class="text-muted m-b-30">Selectionnez les personnes reconnues par l'infecté comme cas contact</p>
                            <div class="table-responsive">
                                <table id="myTable" class="table table-striped">
                                    <thead>
                                        <tr>
                                            <th>Nom</th>
                                            <th>Contact</th>
                                            <th>Action</th>
                                           
                                        </tr>
                                    </thead>
                                      {% for call in call %}
                                    <tbody>
                                        <tr>
                                            <td>{{ call.personneTwo.nom}}</td>
                                            <td>{{ call.personneTwo.contact}}</td>
                
                                            <td><a href=""><button type="button" class="btn btn-primary">Choisir</button></a></td>
                                            
                                        </tr>
                                        
                                    </tbody>
                                       {% endfor %} 
                                </table>
                            </div>
                        </div>
                    </div>
                  </div>
                  </div>
       {% endfor %}
 {% endblock %}

 

